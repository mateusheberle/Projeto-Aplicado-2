{
  "name": "negrao-crud-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Testes end-to-end e smoke da aplicação Ferragens Negrão",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  }
}
